# Mandatory workflow

Read and obey `.ai-quality/agent-policy.md` before editing. Run `pwsh ./aq.ps1 status` at the start and after compaction/resume. Use the active work item in `.ai-quality/active-work-item.txt`. Do not implement before `implementation-authorized`. Require an adapter for every affected stack. In manual mode do not self-approve; in trusted mode use the workflow CLI to self-approve only complete artifacts. Do not claim completion without a passing multi-adapter Full gate and delivery validation.
