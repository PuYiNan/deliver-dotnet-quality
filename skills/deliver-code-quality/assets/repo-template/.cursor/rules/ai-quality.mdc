---
description: Mandatory evidence-based delivery workflow
alwaysApply: true
---

Read `.ai-quality/agent-policy.md`, then run `pwsh ./aq.ps1 status` before editing. Never implement before `implementation-authorized`. Require an adapter for every affected stack. Never self-approve in manual mode; in trusted mode self-approve only through the workflow CLI after readiness checks pass. Never claim completion without passing the complete multi-adapter Full gate and delivery validator.
